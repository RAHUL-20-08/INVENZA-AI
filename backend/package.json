{
  "name": "lost-innovation-backend",
  "version": "1.0.0",
  "description": "Backend API for Lost Innovation Revival Platform",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env app.js",
    "dev": "nodemon --env-file=.env --ignore users.json --ignore audit_logs.json --ignore hackathons.json app.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "mysql2": "^3.23.0",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
